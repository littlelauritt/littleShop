name: Catalog Service CI/CD

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]
    paths:
      - 'littleShop.catalog/**'
      - 'littleShop.Shared/**'
      - 'littleshop.serviceDefaults/**'
      - '.github/workflows/catalog-pipeline.yml'
      - 'tests/catalog-api-test.json'
  pull_request:
    branches: [ "main" ]

env:
  DOTNET_VERSION: '10.0.x'
  REGISTRY: ghcr.io

jobs:
  # -----------------------------------------------------------------
  # JOB 1: BUILD & UNIT TESTS
  # -----------------------------------------------------------------
  build-and-test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup .NET 10 Preview
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}
        dotnet-quality: 'preview'
    
    - name: Restore Dependencies
      run: dotnet restore littleShop.catalog/littleShop.catalog.csproj
    
    - name: Build
      run: dotnet build littleShop.catalog/littleShop.catalog.csproj --no-restore -c Release
    
    - name: Run Unit Tests
      run: dotnet test littleShop.catalog.Tests/littleShop.catalog.Tests.csproj --verbosity normal --logger "trx;LogFileName=test-results.trx"
    
    - name: Publish Test Results
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: test-results-catalog
        path: '**/test-results.trx'

  # -----------------------------------------------------------------
  # JOB 2: INTEGRATION TESTS (usando Aspire AppHost)
  # -----------------------------------------------------------------
  integration-tests:
    needs: build-and-test
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup .NET 10 Preview
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}
        dotnet-quality: 'preview'
    
    - name: Install Aspire workload
      run: dotnet workload install aspire
    
    - name: Restore all projects
      run: dotnet restore littleShop.sln
    
    - name: Build entire solution
      run: dotnet build littleShop.sln -c Release --no-restore
    
    - name: Start Aspire AppHost in background
      run: |
        cd littleShop
        nohup dotnet run --no-build -c Release > aspire.log 2>&1 &
        echo $! > aspire.pid
        echo "⏳ Waiting for Aspire to start..."
        sleep 60
    
    - name: Wait for Catalog API to be ready
      run: |
        echo "🔍 Checking Catalog API health..."
        max_attempts=40
        attempt=0
        while [ $attempt -lt $max_attempts ]; do
          if curl -f -s http://localhost:5002/health > /dev/null 2>&1 || \
             curl -f -s http://localhost:5002/api/v1/products > /dev/null 2>&1; then
            echo "✅ Catalog API is ready!"
            break
          fi
          echo "⏳ Attempt $((attempt+1))/$max_attempts - waiting for Catalog API..."
          sleep 2
          attempt=$((attempt+1))
        done
        
        if [ $attempt -eq $max_attempts ]; then
          echo "❌ Timeout waiting for Catalog API"
          echo "==== Aspire Logs ===="
          cat aspire.log || echo "No Aspire logs available"
          exit 1
        fi
    
    - name: Verify Catalog has products
      run: |
        echo "🔍 Verifying Catalog has products..."
        curl -v http://localhost:5002/api/v1/products || true
    
    - name: Install Newman (Postman CLI)
      run: npm install -g newman
    
    - name: Run Integration Tests
      run: |
        newman run ./tests/catalog-api-test.json \
          --env-var baseUrl=http://localhost:5002 \
          --reporters cli,json \
          --reporter-json-export newman-results.json
    
    - name: Upload Newman Results
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: newman-results-catalog
        path: newman-results.json
    
    - name: Stop Aspire AppHost
      if: always()
      run: |
        if [ -f aspire.pid ]; then
          kill $(cat aspire.pid) || true
        fi
        echo "==== Aspire Logs ===="
        cat aspire.log || echo "No Aspire logs available"

  # -----------------------------------------------------------------
  # JOB 3: DOCKER BUILD & PUSH
  # -----------------------------------------------------------------
  docker-build-push:
    needs: integration-tests
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    permissions:
      contents: read
      packages: write
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Convert repository name to lowercase
      run: |
        echo "LOWERCASE_REPO=${GITHUB_REPOSITORY,,}" >> $GITHUB_ENV
    
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
    
    - name: Log in to GitHub Container Registry
      uses: docker/login-action@v3
      with:
        registry: ${{ env.REGISTRY }}
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    
    - name: Build and Push Docker image
      uses: docker/build-push-action@v5
      with:
        context: .
        file: ./littleShop.catalog/Dockerfile
        push: true
        tags: |
          ${{ env.REGISTRY }}/${{ env.LOWERCASE_REPO }}/catalog-api:latest
          ${{ env.REGISTRY }}/${{ env.LOWERCASE_REPO }}/catalog-api:${{ github.sha }}
        cache-from: type=gha
        cache-to: type=gha,mode=max